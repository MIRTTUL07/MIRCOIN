{
  "name": "decentralized-dns-resolver",
  "version": "1.0.0",
  "description": "A Web3-inspired Decentralized DNS Resolver system with blockchain-like records",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:contracts && npm run build:backend && npm run build:frontend",
    "build:contracts": "cd contracts && npx hardhat compile",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "deploy:contracts": "cd contracts && npx hardhat run scripts/deploy.js --network localhost",
    "test": "cd contracts && npx hardhat test",
    "start:hardhat": "cd contracts && npx hardhat node",
    "install:all": "npm install && cd contracts && npm install && cd ../backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["web3", "dns", "blockchain", "decentralized", "resolver"],
  "author": "dDNS Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
